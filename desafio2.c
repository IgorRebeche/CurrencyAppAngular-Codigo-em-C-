#include <stdio.h>
#include <math.h>

int main(){

    //Receber um numero e uma lista de numeros (sem repeticao)
    //Fazer uma lista com ponteiro
    //Criar Entrada para adicionar no ponteiro

    int valor_Entrada, count = 1;
    int ValorLista[];
    printf("Entre com o numero:");

    while(count){
        printf("Entre com a lista: (Digite o numero)\n(parar a lista aperte S )")
        scanf("%d", valorLista)

        if(valorLista == 120120){
          count = 0;  
        }
        valorLista.put(valorLista);
    }
    

}

void checkNumber(int numero, int lista[]){
    int i = 0;
    for(i = 0; i < lista.length(), i++){
        if(numero == lista[])
    }
}
